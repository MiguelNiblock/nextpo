version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn
    build:
      commands:
        - cd apps/next && yarn build
  artifacts:
    baseDirectory: apps/next/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
